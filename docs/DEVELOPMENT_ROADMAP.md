# AI-администратор для автосервисов — Development Roadmap

> This roadmap reflects the actual state of the repository as verified
> against `prisma/schema.prisma`, `api/`, and `src/` at the time of
> writing. A stage is marked **COMPLETED** only when its models, API
> routes, and frontend pages were confirmed present in the current code —
> not because a prior plan assumed it would be.

## Completed

### 01 — Authentication + Multi-Tenancy

**Status: COMPLETED**

- `Tenant`, `User`, `Business`, `Session` models.
- Registration, login, logout, current-user (`/api/auth/register|login|logout|me`).
- Server-side sessions: Argon2id password hashing, cryptographically random session tokens, HMAC-SHA256-hashed before storage, HttpOnly/SameSite cookies.
- Role architecture: `owner` / `admin` / `manager` (`UserRole` enum), enforced via `requireRole()`.
- Tenant isolation primitives (`withTenant()`, `requireAuth()`) that every later stage reuses unchanged.

### 02 — Business Profile + Services

**Status: COMPLETED**

- Business profile fields (name, description, phone, email, address, `timezone`, `currency`).
- `BusinessWorkingHours` (7-day schedule, created by default at registration).
- `Service` catalog: `Decimal` money fields, currency snapshot, soft-delete via `isActive`.
- Full Service CRUD; manager is read-only on Business profile, working hours, and Services (owner/admin only for writes).

### 03 — Knowledge Base + Business Rules

**Status: COMPLETED**

- `KnowledgeItem` (title/content/category/isActive) with `KnowledgeCategory` enum.
- `BusinessRule` (name/description/category/priority/isActive) with `BusinessRuleCategory` enum, sorted by priority.
- Full CRUD for both; tenant isolation and manager-read-only permissions consistent with Prompt 02.

### 04 — Customers + Vehicles + Leads

**Status: COMPLETED**

- `Customer` (soft-deletable, active-email duplicate protection).
- `Vehicle` (belongs to exactly one Customer, server-verified ownership).
- `Lead` (`customerId` required, `vehicleId`/`serviceId` optional; `LeadStatus`/`LeadSource` enums).
- Search, pagination (`page`/`pageSize`, capped at 100), and filters on all three list endpoints.
- Soft deactivation (`isActive`) for Customer/Vehicle; Lead uses `status` instead (no delete).
- Full tenant isolation, including cross-entity ownership checks (a Vehicle must belong to its stated Customer, etc.).

### 05 — Appointments + Booking

**Status: COMPLETED**

- `Appointment` (`customerId`/`vehicleId`/`serviceId` all required, `startAt`/`endAt` UTC, `AppointmentStatus`).
- Working-hours validation against the existing `BusinessWorkingHours`/`Business.timezone` (no second schedule model), DST-aware via `Intl.DateTimeFormat`.
- Vehicle-scoped conflict detection (standard interval overlap), excluding non-blocking terminal statuses.
- Full customer/vehicle/service ownership + active-state validation.
- Explicit status-transition allow-list; terminal statuses (`COMPLETED`/`CANCELLED`/`NO_SHOW`) never reopen; cancellation via `PATCH { status: "CANCELLED" }`, no `DELETE`.
- Manager granted full create/update access (the first "operational" exception to the manager-read-only pattern).

### 06 — Service History

**Status: COMPLETED**

- `ServiceRecord` (`customerId`/`vehicleId`/`serviceId` required, `appointmentId` optional, `performedAt`, `mileage`, `totalPrice`/`currency`, `workDescription`/`partsDescription`/`recommendations`/`notes`, `isArchived`).
- Non-decreasing mileage per vehicle across non-archived records (checked on create/update/restore, not on archive).
- Appointment-consistency check when `appointmentId` is supplied.
- No hard delete ever — archive/restore only, via `PATCH { isArchived }`.
- Historical-data-survives-deactivation principle applied identically to Lead/Appointment.
- Manager granted full create/update/archive/restore access.

### 07 — Customer Requests + Status History

**Status: COMPLETED**

> The original plan for this stage described `CustomerRequest` as the
> **next** implementation step. Verifying the actual repository shows
> this stage was already fully implemented (Prisma models, migration,
> validation, service/repository layers, API routes, frontend page, and
> tests all present and committed) before this documentation pass. This
> is recorded here as **COMPLETED**, not "current", to match the real
> code — see the discrepancy note at the end of this document.

- `CustomerRequest`: `customerId` (required) + optional `vehicleId`/`serviceId`/`appointmentId`, `source` (`CustomerRequestSource`), `status` (`CustomerRequestStatus`), `subject`, `description`, `requestedDate`, `requestedTimeFrom`/`requestedTimeTo`, `notes`, `createdAt`/`updatedAt`. All fields listed in the original plan are present in `prisma/schema.prisma` exactly as specified.
- `CustomerRequestStatusHistory`: `fromStatus`/`toStatus`/`changedByUserId`/`createdAt`, written atomically with every status change (including the initial `null → NEW`) via an interactive Prisma transaction; read-only through the API, included on `GET /api/customer-requests/:id` only.
- Explicit status-transition allow-list (`NEW → IN_PROGRESS ⇄ WAITING_CUSTOMER → QUALIFIED → CONVERTED`, plus `CLOSED`/`CANCELLED` from any non-terminal state); `CONVERTED`/`CLOSED`/`CANCELLED` are terminal; `CONVERTED` requires a linked `appointmentId` and can never lose it afterward.
- No hard delete — `DELETE /api/customer-requests/:id` is `405`.
- Manager granted full create/update/status-change access.
- Frontend: `/settings/customer-requests` (search, status/source filters, pagination, Customer→Vehicle cascading select, embedded status-history panel on the edit form).
- 91 tests covering schema validation, service-layer business rules, and cross-tenant isolation (part of the project's 538-test suite as of this stage).

### 08 — Conversations + Messages

**Status: COMPLETED**

- `Conversation`: `customerId`/`customerRequestId` (both optional — a conversation can begin before anyone is identified), `channel` (`ConversationChannel` — a label only, no live integration), `status` (`ConversationStatus`: `OPEN`/`CLOSED`, defaults `OPEN`), `subject`, `startedAt`, `lastMessageAt`, `closedAt`. All fields from the original plan are present in `prisma/schema.prisma` exactly as specified.
- `Message`: `direction` (`MessageDirection`: `INBOUND`/`OUTBOUND`), `senderType` (`MessageSenderType`: `CUSTOMER`/`STAFF`/`SYSTEM` — deliberately no `AI` value at this stage), `content` (1–10,000 chars). Append-only: no `PATCH`/`DELETE` endpoint exists for a message.
- Customer/CustomerRequest consistency enforced when both are set on a Conversation (`400` on mismatch); ownership re-verified server-side only for relations actually being set/changed.
- Two-state lifecycle with no history kept for it (`CustomerRequestStatusHistory` remains the only status-history model): closing sets `closedAt`; reopening always clears it; a closed conversation rejects new messages with `409 CONVERSATION_CLOSED`.
- `lastMessageAt` kept atomically consistent with the actual last message via an interactive Prisma transaction, reusing the pattern introduced in Prompt 07.
- The local dev API router (`vite.config.ts`) was generalized to resolve nested dynamic routes (`api/conversations/[id]/messages.ts`), needed for `POST /api/conversations/:id/messages` — a necessary infrastructure change, not a scope expansion; fully backward compatible with every existing route.
- Manager granted full create/update/status-change access, including sending messages.
- Frontend: `/settings/conversations` (list with filters/pagination, create form, detail panel with chronological messages, a send-message form, and Close/Reopen).
- No AI, LLM, embeddings, or external channel integration of any kind — channel values are labels only.
- 65 tests covering schema validation, service-layer business rules, and cross-tenant isolation (part of the project's 603-test suite as of this stage).

### 09 — AI Core

**Status: COMPLETED**

- **Read-only, no actions.** `POST /api/ai/analyze` classifies a test message in the context of an existing Conversation and returns a structured draft — it never creates a Message, never touches Appointment/Customer/Vehicle/ServiceRecord/KnowledgeItem/BusinessRule, and never calls any external channel. Zero new Prisma models — the entire feature reuses existing tables (`Conversation`, `Message`, `Business`, `Service`, `KnowledgeItem`, `BusinessRule`, `Customer`, `Vehicle`, `CustomerRequest`) plus a handful of plain TypeScript/Zod types under `src/server/ai/` that persist nothing.
- **Provider abstraction**: `AiProvider` interface (`src/server/ai/provider.ts`) with two implementations — `OpenAiProvider` (official `openai` SDK, Chat Completions with Structured Outputs / `json_schema` response format) and `MockAiProvider` (deterministic, keyword-based, zero network access). `aiProviderFactory.ts` selects `OpenAiProvider` when `OPENAI_API_KEY` is configured and transparently falls back to `MockAiProvider` when it isn't — this environment has no real key, so `MockAiProvider` is what actually answers every request here, including the real Supabase smoke test.
- **Context builder** (`src/server/ai/contextBuilder.ts`): assembles only business name/description/contact/timezone/currency, active Services, active KnowledgeItems, active BusinessRules, and — only when actually known via the Conversation's `customerId`/linked `CustomerRequest.vehicleId` — a Customer/Vehicle summary. No `tenantId`/`businessId`/internal ids/Customer notes/session data/secrets ever leave this function; every lookup is tenant-scoped through the existing repositories. Service history is deliberately **not** included by default — deciding when it's actually needed requires the intent-based tool-calling mechanism explicitly deferred to Prompt 10+.
- **Prompt architecture**: system instructions, business context, conversation history (last 20 messages), and the current user message are always passed to the provider as four separate fields, never concatenated — the user's own message is the only untrusted value in the whole request, and the system prompt explicitly tells the model to treat it as data, not instructions.
- **Structured output + Zod gate**: the model is constrained via OpenAI's `json_schema` Structured Outputs, and the result is independently re-validated server-side against `aiResultSchema` regardless of what the API claims to guarantee. A result that fails this validation never throws — it degrades to a safe `{ intent: "UNKNOWN", needsHuman: true, ... }` fallback, since the provider did respond, just not usably.
- **Safety layer**: confidence `< 0.50` always forces `needsHuman = true` server-side (never trusting the model's own flag alone); a regex-based check rejects and replaces any draft answer that falsely claims a real action was taken ("I've booked you...", "your appointment is confirmed...") with a safe fallback, also forcing `needsHuman = true`.
- **Errors**: `AI_PROVIDER_UNAVAILABLE` (502) and `AI_CONFIGURATION_ERROR` (500) for genuine provider/config failures where no model output exists at all; `404`/`409` for an unknown/foreign-tenant/closed Conversation, matching existing conventions exactly (reusing Conversation/Message's own `CONVERSATION_CLOSED` code).
- Manager granted the same operational read access as Appointment/Conversation/Customer Requests.
- Frontend: `/settings/ai` — select an existing Conversation, type a test message, Analyze, see intent/confidence/entities/draft answer/needsHuman/reason, with an explicit "Draft only — message was not sent." notice. No chat UI, no send button.
- 75 new tests (678 total): AI result schema validation, request schema validation, mock provider behavior (incl. prompt-injection resistance), safety layer (confidence policy + fabricated-action rejection), context builder (active-only filtering, customer/vehicle inclusion rules, no-secrets-leak assertions), provider configuration/factory selection, full service-layer orchestration (successful analysis, provider failure, malformed provider result, low-confidence escalation, closed-conversation rejection, conversation-not-found), and a dedicated cross-tenant test in `tests/tenantIsolation.test.ts` proving `analyzeMessage` itself returns 404 for a foreign-tenant Conversation without ever building context or calling the provider.
- One new dependency: `openai` (official SDK) — server-side only, verified absent from the built frontend bundle.
- No AI actions, function calling, tools, external channels, RAG, embeddings, vector database, AI logs, or escalation database — exactly as scoped.

### 10 — AI Booking

**Status: COMPLETED**

- **Tool Layer, not direct AI-to-DB access.** Exactly four whitelisted tools — `check_availability`, `create_appointment`, `reschedule_appointment`, `cancel_appointment` (`src/server/ai/tools/`) — are the only way the AI can ever touch booking data. Every tool calls the existing, unmodified `appointmentService.ts` (or a new `checkAvailability()` added to it) — never Prisma directly. No dynamic tool registration, no eval, no arbitrary tool names; an unknown name is rejected by the registry (`tools/registry.ts`) before anything executes.
- **Zero new Prisma models, zero new migrations.** `checkAvailability()` reuses `BusinessWorkingHours` (Prompt 02) and the existing per-vehicle conflict check (Prompt 05); the only new server-side primitive is `businessLocalToUtc()` (`src/server/lib/timezone.ts`), the DST-safe inverse of Prompt 05's `toBusinessLocalDateTime()`, needed to turn a candidate local time back into a real UTC instant for slot generation.
- **Two-phase booking is enforced in code, not just prompted.** A deterministic, regex-based `isExplicitConfirmation()` (`src/server/ai/confirmation.ts`) gates `create_appointment`, `reschedule_appointment`, and `cancel_appointment` against the *current* customer message before any business logic runs — a model deciding to call a mutating tool is never itself sufficient authorization. Positive/negative phrasing is covered by 25 dedicated tests (`tests/confirmation.test.ts`).
- **Entity allow-list — the anti-injection mechanism.** `AiToolAllowedEntities` (derived server-side from the resolved `AiBusinessContext` — the conversation's own known customer/vehicle/upcoming appointments — never from model output) additionally restricts every mutating tool call to the entity this conversation actually knows about; a same-tenant but unrelated customer/vehicle/appointment id is rejected as `FORBIDDEN` even though it would otherwise pass tenant scoping. This closes a real gap found while writing this stage's own prompt-injection tests (a message merely containing a confirmation phrase was, before this fix, otherwise sufficient).
- **Concurrency**: `create_appointment` always re-validates against the real, current database state via the existing conflict check — an earlier `check_availability` result (this request or a prior one) is never assumed still valid. Verified by both a unit test and the real-database smoke test (identical slot booked twice → the second attempt gets a real `409`-derived `APPOINTMENT_CONFLICT`, not a stale-cache false positive).
- **Idempotency**: no new persistent model was introduced for this (deliberately, per this stage's own instruction not to invent schema to look complete) — the practical safeguard is the same real-time conflict re-check above. A dedicated request-level idempotency key is a known, documented limitation, not an oversight (see the Prompt 10 completion report for detail).
- **Server-controlled tool-calling loop** (`analyzeMessage` in `aiService.ts`): bounded at a maximum of 3 tool calls per `/api/ai/analyze` request, with the last allowed round reserved to let the provider see the final tool result rather than being cut off one round early. Exceeding the bound degrades to a controlled `needsHuman: true` result, never an infinite loop or a 5xx.
- **Every tool argument is Zod-validated** (`tools/schemas.ts`) before execution; tenant/business context always comes from `requireAuth()` via `AuthContext`, never from anything the model supplies (including if a compromised model tries to smuggle `tenantId`/`businessId` into tool arguments — the schemas have no such fields, so they're silently stripped and never reach any query). Tool results are always one of exactly two shapes — `{success:true, tool, data}` or `{success:false, tool, errorCode, message, retryable?}` — never a raw Prisma object, never `tenantId`/`businessId`.
- **`POST /api/ai/analyze`'s contract is unchanged** — same `{conversationId, message}` request; the additive, optional `toolExecutions` field only appears when a tool actually ran this request. `analyze` still never auto-creates a Message, even after a successful booking.
- Manager granted the same operational access as every other booking-adjacent domain.
- Frontend: `/settings/ai` extended with a visible "tool execution is real" warning banner and a tool-execution results panel (tool name, success/failure, availability slots, or the resulting appointment's id/status/start/end) — sending a message to the customer remains disabled.
- Tests: `tests/aiTools.test.ts` (36), `tests/aiToolSchemas.test.ts` (18), `tests/confirmation.test.ts` (25) are new; `tests/appointmentService.test.ts`, `tests/aiContextBuilder.test.ts`, `tests/aiService.test.ts`, `tests/mockAiProvider.test.ts` were extended; `tests/tenantIsolation.test.ts` gained a dedicated "AI Booking Tools" section covering all four tools against a foreign tenant. 811 tests total, all passing, zero mocked-OpenAI-key dependency (`MockAiProvider` drives every documented scenario, including two explicit prompt-injection cases).
- A full real-Supabase smoke test (two real tenants, no mocks) verified: real computed availability slots against real working hours; a real `create_appointment` through the real Appointment Service (row exists, correctly scoped, correct relations/time/status); a real conflict on re-booking the identical slot; a real reschedule and a real cancellation (row still present, `status: CANCELLED`, never deleted); complete tenant-isolation rejection of all four tools from a second tenant; and full cleanup with zero rows remaining afterward.
- No customer/vehicle auto-creation, no invented/ambiguous Service resolution, no price modification, no AI logs/audit, no escalation model, no external channels, no RAG/embeddings/vector DB, no multi-agent/autonomous agent behavior — exactly as scoped.

### 11 — AI Customer Support

**Status: COMPLETED**

- **Service History enters the AI context for the first time.** `contextBuilder.ts` now includes `serviceHistory` — the known vehicle's most recent 10 non-archived `ServiceRecord`s (newest-first, same gating as `upcomingAppointments`: only when a vehicle is already resolved via the Conversation's linked CustomerRequest). **Zero new Prisma models, zero new migrations** — reuses the existing `serviceRecordRepository.list()` unchanged. Exposed fields: `performedAtLocal`, `serviceName`, `mileage`, `totalPrice`, `currency`, `workDescription`, `partsDescription`, `recommendations`, `notes` — no `id`/`customerId`/`vehicleId`/`serviceId`/`appointmentId`, matching Prompt 09's "no unnecessary internal identifiers" principle.
- **History is fact, never diagnosis.** The system prompt (rule 16) and a new deterministic safety check (`applyDefinitiveDiagnosisCheck` in `safety.ts`) both enforce that Service History can be *stated* ("15 августа заменили масло при пробеге 82 400 км") but never used to *conclude* a current symptom's cause ("значит, колодки снова нужно менять" is refused) — the AI acknowledges the symptom, cites history as fact where relevant, and recommends an in-person inspection instead.
- **Grounded answers, not placeholders.** `MockAiProvider`'s customer-support path (`classifyCustomerSupport()`) was rewritten from Prompt 09's generic "ask a human" deflections into real, source-grounded answers: `SERVICE_INQUIRY` (name/description/price/duration from `Service`), `PRICE_INQUIRY` (exact `priceFrom`/`priceTo`, or an honest "no price on file" — never an estimate), `WARRANTY_INQUIRY` (grounded in `BusinessRule`/`KnowledgeItem`, in that priority order), `SERVICE_HISTORY_INQUIRY` (real `ServiceRecord` facts, or an honest "no records"), general questions (matched against `BusinessRule` first — lower `priority` number wins on a conflict — then `KnowledgeItem`), and an honest "I don't have enough information to confirm that" for anything nothing real supports, rather than a guess.
- **Fabricated-escalation protection.** A second new deterministic safety check (`applyFabricatedEscalationCheck`) catches a draft answer that falsely claims a human/manager was already contacted ("я передал ваш вопрос менеджеру") — no escalation mechanism exists yet (that's Prompt 12), so `needsHuman: true` remains only a signal on the result, never a claimed action.
- **Prompt-injection defense extended**: `PROMPT_INJECTION_PATTERNS` (mock) and system-prompt rule 13 now also cover "pretend this vehicle is mine," "ignore the business rules," and "the system says you can access all customers," in addition to Prompt 09's original "ignore instructions" patterns — verified with the four exact example messages from the spec, none of which are complied with.
- **Tenant/customer isolation for history is structural, not a special case**: `buildAiContext` resolves the vehicle (and therefore its history) exclusively through the current Conversation's own tenant-scoped `customerId`/`CustomerRequest.vehicleId` chain — there is no code path by which another tenant's or another same-tenant customer's history could ever be loaded, verified by dedicated context-builder unit tests and a live two-tenant, two-customer check against the real database.
- **`POST /api/ai/analyze`'s contract is unchanged**, and it remains fully read-only: no `Message`/`Conversation`/`ServiceRecord`/`Customer`/`Vehicle`/`KnowledgeItem`/`BusinessRule` write of any kind — verified directly against the real database (row timestamps compared before/after).
- Manager granted the same operational read access as every other AI-adjacent domain.
- Frontend: `/settings/ai`'s description text updated to mention Service History grounding; the existing intent/confidence/entities/answer/needsHuman/reason/tool-execution display already covers everything this stage needed to demonstrate.
- Tests: `tests/aiContextBuilder.test.ts` gained a "serviceHistory" section (6 tests); `tests/aiSafety.test.ts` gained coverage for both new safety checks (8 tests); `tests/mockAiProvider.test.ts` gained an "AI Customer Support" section covering all 11 documented scenarios plus the 4 prompt-injection examples (18 tests). 843 tests total, all passing, zero mocked-OpenAI-key dependency.
- A full real-Supabase smoke test (two real tenants, two customers in tenant A, no mocks) verified: Service History read from the real database and correctly bound to the right vehicle; the archived record excluded; another same-tenant customer's history never appearing; another tenant's history never appearing; `analyzeMessage` producing a grounded `SERVICE_HISTORY_INQUIRY` answer citing the real date/mileage; a non-diagnostic `VEHICLE_PROBLEM` response; zero `Message`/`Conversation`/`ServiceRecord` writes; cross-tenant `analyze` rejection with `404`; and full cleanup with zero rows remaining across all 12 affected tables.
- No Human Escalation, no AI Logs/audit, no external channels, no customer/vehicle/service auto-creation, no new AI tools, no RAG/embeddings/vector DB, no new Prisma models — exactly as scoped.

### 12 — Human Escalation

**Status: COMPLETED**

- **`needsHuman: true` is now a real, persisted, tenant-isolated workflow, not just a response flag.** A new `AiEscalation` model (`prisma/schema.prisma`, migration `20260908155027_ai_escalation_foundation`) turns the AI Core's existing signal into a durable staff handoff record. This is the first AI-related stage since Prompt 09 to add a database model — deliberately, since a real human-handoff *state* cannot exist without one.
- **Idempotency is a real database constraint, not just an application check.** `AiEscalation.activeConversationId` mirrors `conversationId` only while status is `OPEN`/`IN_PROGRESS`, forced back to `null` on resolve/cancel; a `@@unique([tenantId, businessId, activeConversationId])` index gives Postgres genuine "at most one active escalation per conversation" enforcement — Postgres treats every `NULL` as distinct, so any number of resolved/cancelled rows can coexist for one conversation, but never two simultaneously-active ones. Prisma's schema DSL cannot express a partial/filtered unique index directly, so this nullable-column technique is the fully-declarative equivalent. `escalationService.createOrReuseActiveEscalation()` reads the active row first as a fast path, and on a genuine concurrent-insert race (P2002 unique-constraint violation), re-reads and returns the now-current active row instead of failing or duplicating — verified by a dedicated unit test that constructs a real `Prisma.PrismaClientKnownRequestError`.
- **AI integration is a single, precisely-gated point** in `aiService.ts`'s `analyzeMessage()`: escalation creation/reuse happens only when the final result both (a) came from real structural + safety validation (`aiResultSchema.safeParse` succeeded — a malformed provider result or an exceeded tool-call limit never reaches this gate) and (b) is not itself a safety-layer *rejection* (a caught fabricated booking/diagnosis/escalation claim, identified by its `AI_SAFETY_REJECTION` reason prefix — the safety layer already fully contained that problem, so it is not additionally treated as evidence a human is needed) and (c) `needsHuman === true`. Provider failures (`AI_PROVIDER_UNAVAILABLE`/`AI_CONFIGURATION_ERROR`) never reach this point at all — they throw out of `analyzeMessage` before any `AiResult` exists.
- **`reason`/`summary` are always server-derived**, never client- or model-trusted raw text: `deriveEscalationReason()` takes the validated `AiResult.reason`, trims it, and falls back to a fixed `"AI requires human assistance"` string when absent — bounded to 500 characters. `deriveEscalationSummary()` builds a fixed-template safe sentence from that already-derived reason — never the raw provider payload, never chain-of-thought (none is ever requested from the model).
- **Status machine**: `OPEN → IN_PROGRESS` (claim) `→ RESOLVED`; `OPEN`/`IN_PROGRESS → CANCELLED`; both `RESOLVED` and `CANCELLED` are terminal (no transition out — including no reopening, deliberately not implemented since Prompt 10's `AppointmentStatus` precedent and Step 19's own re-analysis rule make a fresh escalation the correct response instead). Same-status requests are a no-op, matching `appointmentService.ts`'s existing `assertValidTransition` convention exactly. `resolvedAt` is set only by `RESOLVED` (server time, never client-supplied); `CANCELLED` deliberately never sets it — `resolvedAt` means "actually resolved," not "closed for any reason."
- **Claim is atomic** via a scoped `updateMany` whose `WHERE` clause (`status: 'OPEN', assignedUserId: null`) is itself the concurrency guard — two staff members claiming the same escalation at the same instant can never both succeed; the loser's `updateMany` matches zero rows and the service layer reports a real `409 ESCALATION_ALREADY_ASSIGNED`, distinct from the idempotent no-op when the same user re-claims their own escalation.
- **Permissions**: owner/admin/manager all get identical list/view/claim/resolve/cancel access — the same operational exception already established for Appointment/Conversation/AI Core (README.md "Roles"). Explicit reassignment (Step 25) was deliberately not implemented — claiming alone is sufficient for this stage.
- **API**: `GET /api/escalations` (paginated, filterable by status/priority/assignedUserId/unassignedOnly/customerId/conversationId, ordered by priority — Postgres's native enum-declaration ordering already yields `URGENT > HIGH > NORMAL > LOW` with zero application-level sorting — then newest first), `GET /api/escalations/:id`, `POST /api/escalations/:id/claim`, `POST /api/escalations/:id/resolve`, `POST /api/escalations/:id/cancel`. **Deliberately no `POST /api/escalations`** (plain create) **and no generic `PATCH /api/escalations/:id`** — the only way an escalation is ever created is `aiService.ts → escalationService.createOrReuseActiveEscalation()`, and every state change goes through one of the three explicit action endpoints, never a second, parallel way to do the same thing.
- **`POST /api/ai/analyze`'s contract is unchanged**; an additive, optional `escalation?: {id, status}` field appears only when `needsHuman === true` and a real row was created or reused — never `tenantId`/`businessId`/internal metadata. A genuine escalation-creation failure (e.g. a real database error after the idempotency fast path and the P2002-recovery path both fail) propagates as a controlled `500 ESCALATION_CREATION_FAILED`, never a silent `needsHuman: true` with a missing escalation reference.
- **Priority is always `NORMAL`** for every AI-created escalation — no safe, tested signal for elevating it exists yet (per spec, preferring a flat, honest priority over hallucinated urgency); the `LOW`/`HIGH`/`URGENT` enum values exist in the schema for future use but nothing in this stage ever assigns them.
- Frontend: `/settings/escalations` — list with status/priority/unassigned filters, a detail panel (customer/conversation/assigned-staff summary, the AI's reason and summary, Claim/Resolve/Cancel buttons gated by both status and role), linked from `/settings/ai`'s result panel when an analyze call actually creates or reuses one. No Kanban board, no analytics, exactly as scoped.
- Tests: `tests/escalationService.test.ts` is new (35 tests: idempotency incl. the real P2002-recovery path, the full status machine, atomic claim/conflict, permissions); `tests/aiService.test.ts` gained a dedicated "Human Escalation integration" section plus escalation assertions on the existing malformed-result/tool-limit/safety-rejection/confidence-policy tests (net +13 tests); `tests/tenantIsolation.test.ts` gained an "AI Escalation" section (9 tests: repository-level tenant/business scoping, and service-level 404s for view/claim/resolve/cancel across tenants). 900 tests total, all passing.
- A full real-Supabase smoke test (two tenants, a second staff user in tenant A, no mocks) verified: `needsHuman=false` creates nothing; `needsHuman=true` creates a real, correctly-linked-and-scoped row; repeated analysis reuses the same active escalation; claim is atomic and a second user cannot steal it; resolve sets `resolvedAt`; a fresh `needsHuman=true` after resolution — and again after cancellation — each creates a genuinely new escalation; provider/configuration failures create nothing; cross-tenant view/claim/resolve/cancel are all rejected with `404` and leave the real row untouched; zero unintended `Message`/`Conversation`/`ServiceRecord` writes; full cleanup with zero rows remaining across all 7 affected tables.
- **Business isolation** (Step 12) is enforced in code exactly like every other domain (`withTenant()`-scoped queries always include `businessId`), but is not independently exercisable against the real database today — every tenant in this application currently has exactly one `Business`, so a true two-business-same-tenant scenario doesn't exist to smoke-test yet. Documented as a real, code-level guarantee (verified by unit tests against the mocked query shape) rather than a live-database-verified one, per this stage's own instruction not to claim a stronger guarantee than what was actually checked.
- No Telegram/WhatsApp/email/SMS/external notifications, no AI Logs/audit, no analytics, no realtime/websockets, no new AI tools, no customer/vehicle auto-creation, no RAG/embeddings/vector DB, no multi-agent — exactly as scoped.

### 13 — AI Logs / Audit

**Status: COMPLETED**

- **A safe, minimal technical audit trail, not a transcript or a dashboard.** A new `AiLog` model (`prisma/schema.prisma`, migration `20260908174017_ai_logs_audit_foundation`) records what actually happened for seven operations (`AiLogOperation`: `AI_ANALYZE`, `AI_TOOL_EXECUTION`, `AI_ESCALATION_CREATE`/`REUSE`/`CLAIM`/`RESOLVE`/`CANCEL`) and six outcomes (`AiLogOutcome`: `SUCCESS`, `ESCALATED`, `REUSED`, `FAILED`, `REJECTED`, `NO_ACTION`) — never chain-of-thought, hidden reasoning, a raw provider response, a full prompt, a full `Message`/transcript, or any secret/credential. `aiLogService.ts` is the sole gatekeeper deciding what is safe to persist; the AI itself never gets direct Prisma access.
- **Data minimization is enforced in code, not just by convention.** `sanitizeMetadata()` accepts only a fixed whitelist (`provider`, `toolCallCount`, `errorCode`, `statusCode`, `confirmationRequired`, `retryable`); any other key is silently dropped, nested objects/arrays are dropped entirely (never serialized), and surviving string values are truncated to 200 characters. `sanitizeReason()` truncates to 500 characters and returns `null` for blank input — mirroring `escalationService.ts`'s existing `deriveEscalationReason()` convention from Prompt 12.
- **The audit reflects actual events, never the model's intentions.** A new `attempted: boolean` field on `ToolResult`'s failure variant (`src/server/ai/types.ts`) distinguishes a real execution attempt (the underlying `appointmentService.ts` function was actually called and threw) from a gate rejection that never reached it (confirmation missing, Zod validation failed, entity not in this conversation's allow-list) — necessary because `errorCode` alone is not a reliable proxy (`appointmentService.ts` can genuinely throw the same generic `INVALID_INPUT` code a gate rejection also produces). `aiService.ts`'s tool-calling loop logs `AI_TOOL_EXECUTION` only when `result.success || result.attempted` — a blocked/rejected call the model merely requested is never logged as if it ran.
- **AI_ANALYZE logging integrates after full validation, never before.** `aiService.ts`'s `analyzeMessage()` computes exactly one `analyzeOutcome` per call, only after structural (`aiResultSchema.safeParse`) and safety (`applySafetyLayer`) validation have both completed: `FAILED` for a tool-call-limit-exceeded or malformed/unparseable result (no trustworthy validated output exists to log as anything else), `REJECTED` for a caught safety-layer rejection, `ESCALATED` when `needsHuman` is genuinely eligible and true, `SUCCESS` otherwise. A provider/configuration failure (`AI_PROVIDER_UNAVAILABLE`/`AI_CONFIGURATION_ERROR`) never produces an `AiResult` at all, but does write a durable `AI_ANALYZE/FAILED` record before rethrowing — a genuine technical failure is still worth an audit trail, never a false `SUCCESS`.
- **Escalation create-vs-reuse is never conflated.** `escalationService.ts`'s `createOrReuseActiveEscalation()` — the one place that genuinely knows which happened — writes `AI_ESCALATION_CREATE`/`SUCCESS` on a real insert, `AI_ESCALATION_REUSE`/`REUSED` on both the fast-path reuse and the P2002-concurrent-race-recovery path, and deliberately writes nothing at all when creation genuinely fails (no real `escalationId` exists to attach, and an inaccurate record is worse than a missing one). The three staff actions (`claimEscalation`/`resolveEscalation`/`cancelEscalation`) log `AI_ESCALATION_CLAIM`/`RESOLVE`/`CANCEL` with the acting `actorUserId` only after the real, non-idempotent state transition succeeds — an idempotent no-op (already claimed by you, already resolved, already cancelled) writes nothing, since no real event occurred.
- **Atomicity strategy (business action vs. audit log), stated explicitly per this stage's own instruction:** the Booking Service and Escalation Service were never rewritten for logging. `aiLogService.ts`'s `writeLog()` wraps every `aiLogRepository.create()` call in try/catch and never rethrows — the real business action (booking, escalation, staff transition) always happens and is durable *first*; the audit write is attempted only afterward, and its failure is reported via `logger.error('ai_log_write_failed', ...)` rather than rolling back or masking the already-successful business action. This was verified empirically, not just designed: during test-suite debugging, a real (accidental) Supabase foreign-key failure on every `writeLog()` call was silently and correctly swallowed without breaking any caller.
- **API**: `GET /api/ai-logs` (paginated, filterable by `operation`/`outcome`/`conversationId`/`escalationId`/`dateFrom`/`dateTo`, `createdAt DESC` then `id DESC`), `GET /api/ai-logs/:id`. **Deliberately no `POST /api/ai-logs`** — a log row is only ever written by `aiLogService.ts` itself, never from a client request; `tenantId`/`businessId`/`actorUserId`/`outcome`/`provider`/`toolSuccess`/`confidence`/`needsHuman`/`createdAt` are always server-derived. A foreign-tenant or foreign-business log id returns a plain `404`, never revealing that the row exists elsewhere.
- **Permissions**: owner/admin/manager — same operational access already established for every other AI/staff-facing domain.
- Frontend: `/settings/ai-logs` — a filterable, paginated list (operation, outcome, date range) plus a safe detail panel (intent/confidence/needsHuman/tool/reason, conversation/escalation links, actor, whitelisted metadata only). Explicitly not a dashboard — no charts, no aggregates, no metrics; that's Prompt 14.
- Tests: `tests/aiLogService.test.ts` is new (17 tests: metadata whitelist/truncation, reason truncation, non-throwing write-failure behavior, permission/tenant-scoping on read); `tests/aiService.test.ts` gained a dedicated "AI Logs / Audit" section (net +11 tests: exactly-one-record-per-outcome for every branch, no false `SUCCESS` on any failure path, tool logs only for genuinely-attempted calls); `tests/escalationService.test.ts` gained an equivalent section (+13 tests: create-vs-reuse never conflated, no audit on any idempotent no-op or failed transition); `tests/tenantIsolation.test.ts` gained an "AI Log" section (+6 tests: repository- and service-level tenant/business scoping, 404 across both tenant and business boundaries). 947 tests total, all passing.
- A full real-Supabase smoke test (two tenants, two businesses, no mocks, 28 checks) verified: independent per-tenant `AI_ANALYZE` logging; cross-tenant and cross-business (same-tenant) log reads both correctly `404`; a genuine escalation create followed by a genuine reuse produces exactly one `AI_ESCALATION_CREATE` and a separate `AI_ESCALATION_REUSE`, never two creates; a provider failure produces a `FAILED` record and no false `SUCCESS`; a real `check_availability` tool call produces exactly one `SUCCESS` tool log; an invalid-service-id tool call (a genuine, attempted `NOT_FOUND` failure) produces exactly one `FAILED` tool log with only a safe `errorCode`; claim/resolve/cancel each produce their own correctly-attributed audit row; the full AI-generated answer text is never persisted into any log row; every persisted `metadata` object contains only whitelisted keys; full cleanup (ordered to respect `AiLog`'s `Restrict` foreign keys) with a post-cleanup re-count confirming zero rows remain.
- No Dashboard/Analytics UI, no external notifications, no realtime/websockets, no RAG/embeddings, no new AI tools, no customer/vehicle auto-creation, no Team Management, no full history of all business entities — exactly as scoped.

### 14 — Dashboard / Analytics

**Status: COMPLETED**

- **Every number is a real, server-side aggregation — zero new Prisma models, zero migrations.** `src/server/services/analyticsService.ts` + `src/server/repositories/analyticsRepository.ts` (new files, following the existing route → service → repository → Prisma layering exactly) read the same tables every other domain already uses (`CustomerRequest`, `Conversation`, `Message`, `AiLog`, `AiEscalation`, `Appointment`, `ServiceRecord`, `Service`, `Customer`, `Vehicle`) via `count`/`groupBy`/`aggregate` — never a `findMany()` of a large table reduced in JavaScript, and never a per-day query loop (a bounded ~20 Postgres round-trips per dashboard request, run in parallel via `Promise.all`, regardless of period length).
- **Period is interpreted in `Business.timezone`, never the server's own clock or a client-supplied zone.** `computePeriodBounds()` resolves "today" via the existing `toBusinessLocalDateTime()` (Prompt 10), does pure calendar-date arithmetic on the resulting `"YYYY-MM-DD"` key (never touching a timezone again), and converts the computed local start/end days back to real UTC instants via the existing `businessLocalToUtc()` — the same DST-safe, ICU-backed pair of helpers Prompt 10's availability slots already relied on, reused unchanged. `today`/`7d`/`30d`/`90d` are supported (spec's minimum plus the free `today` case), default `30d`; the query window is always the half-open `[start, end)` interval already used by every existing `dateFrom`/`dateTo` filter in this codebase.
- **Timezone-aware day-bucketing is the one place raw SQL was used**, because Prisma's query builder cannot express "group by the Business-local calendar day of a UTC timestamp" — `` `(("createdAt" AT TIME ZONE 'UTC') AT TIME ZONE $timezone)::date` `` (first reinterpreting the stored naive-UTC `TIMESTAMP(3)` column as a real instant, then converting that instant to local wall-clock time). Every interpolated value (`tenantId`, `businessId`, `timezone`, the UTC bounds) is a bound parameter via Prisma's tagged-template `$queryRaw` — never string concatenation; verified by a dedicated test asserting the raw SQL text itself never contains a tenant id or timezone string, only the bound-parameters array does.
- **Client input is exactly one field**: `?period=`. `tenantId`/`businessId` have no parameter to accept them through at all — every repository function takes `(tenantId, businessId, ...)` sourced exclusively from `requireAuth()`'s `AuthContext`, the same pattern every other endpoint in this codebase already follows.
- **Metrics delivered**: CustomerRequests (total + status breakdown), Conversations (total + channel/status breakdown), Messages (total/inbound/outbound), AI analyses (total/successful/failed/rejected/escalated, average confidence over non-null values only, intent breakdown, full outcome breakdown, success rate, escalation rate), Tool executions (total/successful/failed, per-tool breakdown, success rate — genuine execution log rows only, per Prompt 13's `attempted` guarantee), Escalations (total + status + priority breakdown, scoped to `createdAt`, never `resolvedAt`), Appointments (total + status breakdown, completed/cancelled/no-show), Service History (total records created, revenue **by currency**, archived records excluded from revenue only), Services/Customers/Vehicles (a current active-count snapshot alongside a period-scoped "new" count — the two are explicitly never conflated), a CustomerRequest→Appointment conversion rate, and four business-local daily time series (CustomerRequests, AI analyses, Escalations, Appointments) with every day in the period present, zero-filled where there's no data.
- **Every rate is a documented, consistent policy, never `NaN`**: `successRate`/`escalationRate`/`tools.successRate`/`conversion.customerRequestToAppointment` are 0..1 fractions that return `0` (never `null`/`NaN`) when their denominator is 0 — a single `rate()` helper enforces this everywhere.
- **Conversion uses the one reliable, direct relation that actually exists** — `CustomerRequest.appointmentId` (a real FK, set only by `customerRequestService.ts` on genuine conversion) — never a `customerId`+time-window heuristic. It is a cohort rate: both the numerator and denominator are CustomerRequests **created** in the selected period, so a request opened near the end of the window that converts just after it is not counted as converted for that window — a documented limitation, not a bug.
- **Revenue is never fake/float money**: grouped by currency via Prisma's `_sum` aggregate, serialized with the same `.toFixed(2)` fixed-point-string convention every other Decimal field in this codebase already uses (`dto.ts`) — never summed across currencies, and the entire API response (including every revenue string) is validated against a Zod schema (`dashboardResponseSchema`) before it's ever sent.
- **API**: `GET /api/dashboard?period=today|7d|30d|90d` (default `30d`) — owner/admin/manager only, the same operational exception every other AI/staff-facing domain already has. An invalid period is a real `400`, never a silent fallback.
- Frontend: the existing `/dashboard` route (Prompt 02) was extended, not duplicated — a period selector, KPI cards, an AI section (success/escalation rate, average confidence, intent breakdown), an Operations section (appointment/escalation/tool breakdowns), a Service section (revenue by currency, active customers/vehicles), and a dependency-free CSS bar-chart time-series section. React only ever calls the one endpoint and renders what it returns — no client-side `groupBy`, rate calculation, or raw-row fetch of any kind.
- Tests: `tests/analytics.schemas.test.ts` is new (10 — period parsing/defaulting/rejection, response-schema acceptance/rejection incl. a rejected floating-point revenue value); `tests/analyticsService.test.ts` is new (28 — every KPI computation, rate edge cases, revenue serialization, and dedicated UTC/Europe-Moscow/Asia-Almaty timezone-boundary tests against the exported `computePeriodBounds()`); `tests/tenantIsolation.test.ts` gained an "Analytics / Dashboard" section (+18 — every repository function's tenant/business scoping, the `isArchived: false` revenue filter, and the raw day-bucket queries' parameter binding). 1003 tests total, all passing.
- A full real-Supabase smoke test (two tenants, rich synthetic data — multiple statuses/currencies/priorities/an archived record/active+inactive customers and vehicles, no mocks, 38 checks) verified every KPI/rate/breakdown against hand-computed expected values, confirmed the archived `ServiceRecord` is excluded from revenue but still counted in `serviceHistory.total`, confirmed tenant A/B dashboards never cross-contaminate, and — the core of this stage — took the exact same UTC instant (22:00 UTC) and confirmed it lands in a genuinely different local calendar day under UTC vs. Europe/Moscow (+3) vs. Asia/Almaty (+5), each verified against a real, persisted `Business.timezone` update; full cleanup with a post-cleanup re-count confirming zero rows remain.
- **No business logic was changed.** No appointment/escalation/customer-request status machine, AI safety check, AI provider, or AI tool was touched — this stage only reads existing tables.
- **0 database migrations** — the preferred, spec-mandated outcome; no new Prisma model, no denormalized counter table, no materialized view.
- No Team Management, no external channels/notifications, no realtime/websockets, no RAG/embeddings, no new AI capabilities, no billing/subscriptions, no forecasting/predictive analytics/ML — exactly as scoped.

### 15 — Team Management

**Status: COMPLETED**

- **One additive migration**: `User.isActive Boolean @default(true)` (migration `20260909075339_team_management_user_status`) — the only schema change this stage needed. `User` already had no `businessId` column at all (every tenant has exactly one `Business`, so tenant-scoping alone is the correct and complete isolation boundary for team members — the same invariant every other stage's docs already record); `name`/`email`/`role` were reused exactly as they already existed, deliberately never duplicated into a `firstName`/`lastName` split the original prompt suggested.
- **Multiple active owners are a first-class, explicitly-supported state**, not an edge case: `owner → owner` creation is allowed, and the *only* constraint is that an operation may never bring the active-owner count to `0`. Every other owner-vs-owner interaction (profile editing, in particular) is unrestricted.
- **A precise, authoritative permission matrix** (`teamService.ts`): owner manages everyone (subject to the last-owner rule below); admin manages admin/manager only — never touching an owner's profile, role, or active status, and never creating or promoting to owner; manager is read-only (list/view) everywhere, enforced with a real `403` for every mutation endpoint regardless of what the frontend hides.
- **Profile updates are structurally incapable of changing authorization state.** `updateTeamMemberProfile()` only ever reads `name`/`email` off its input — not "validates and rejects the rest," but never even looks at any other field, so `role`/`isActive`/`tenantId`/`businessId`/`passwordHash` have no path into Prisma through this function no matter what a client sends (the Zod schema also silently strips them beforehand, a second, independent layer). Verified end-to-end against the real database: a request that also carried `role: "owner"`, `isActive: false`, and foreign `tenantId`/`businessId` values changed only the name.
- **Last-owner protection is genuinely race-safe**, not a naive count-then-update: `teamRepository.ts`'s `changeRole()`/`deactivate()` run inside a Postgres `SERIALIZABLE` transaction that counts other active owners and aborts the whole transaction if the count would reach zero — real predicate-lock-based write-skew detection (the classic "two concurrent requests each individually see one other active owner, but committing both leaves zero" anomaly), not just an isolation level chosen for show. A genuine serialization conflict (Postgres error `P2034`) is caught and reported as a distinct, honest `409 CONCURRENT_UPDATE` — never misrepresented as a last-owner violation it didn't actually detect.
- **Self-protection is checked before, and independent of, the last-owner rule**: a user can never deactivate or change the role of their own account through Team Management, full stop — verified by triggering self-deactivation *while being the sole remaining active owner* and confirming the error is `CANNOT_DEACTIVATE_SELF`, not `LAST_OWNER_REQUIRED` (proving the self-check genuinely runs first, not just that both would have blocked it).
- **Deactivation invalidates sessions atomically** with the `isActive` flip — `teamRepository.deactivate()`'s transaction also deletes every `Session` row for that user in the same commit, and `requireAuth()` independently re-checks `isActive` on every request as a second guarantee. `loginUser()` rejects a deactivated account with `403 USER_INACTIVE`, checked only after password verification (an attacker without the password learns nothing about the account's state). Verified live: an old session cookie is rejected with `401` immediately after deactivation, login as the deactivated user fails, and after reactivation a fresh login produces a session that works again.
- **`AiEscalation.assignedUserId`/`AiLog.actorUserId` are untouched by this stage** — deactivation never triggers reassignment (the spec explicitly forbids inventing that logic); the existing `SetNull` relations only matter for an actual row deletion, which Team Management never performs.
- **API**: `GET /api/team`, `GET /api/team/:id`, `POST /api/team`, `PATCH /api/team/:id` (profile fields only), `POST /api/team/:id/role`, `POST /api/team/:id/activate`, `POST /api/team/:id/deactivate` — role and status changes deliberately have no path through the generic `PATCH`.
- Frontend: `/settings/team` — a table (name/email/role/status/created/actions), a create form (role options narrowed to what the current user may actually assign), a profile-edit form, an inline role-change select, and activate/deactivate with a confirmation step for deactivation. The frontend narrows options for UX only; the backend is the actual boundary in every case.
- Tests: `tests/teamService.test.ts` is new (41 — the full permission matrix, every self-protection and last-owner scenario, the multiple-owners profile-editing matrix from the prompt's own worked example, duplicate-email handling, and the P2034→409 translation); `tests/tenantIsolation.test.ts` gained a "Team Management" section (+15); `tests/authService.test.ts` and `tests/requireAuth.test.ts` each gained a deactivated-user rejection test. 1056 tests total, all passing.
- A full real-Supabase smoke test (two tenants — one with two owners, an admin, and a manager — no mocks, 36 checks) verified every item in the prompt's own 24-point smoke checklist: mutual owner-to-owner profile editing, admin-cannot-manage-owner across all five mutation endpoints, manager mutation rejection, the complete login → deactivate → old-session-401 → login-rejected → reactivate → new-session-works cycle, last-owner rejection from a genuinely different actor (not just self-protection), cross-tenant `404`s on all four mutation endpoints, DTO field absence, and the profile-update privilege-escalation attempt changing nothing but the allowed field; full cleanup with a post-cleanup re-count confirming zero rows remain.
- **No existing authentication/session architecture was replaced** — `isActive` is checked at the two existing points (`loginUser()`, `requireAuth()`) that already resolve a fresh `User` row per request/login; no caching, no token-embedded role, nothing new to invalidate beyond the `Session` table that already existed.
- No external channels, no realtime/websockets, no new AI capabilities, no billing/subscriptions, no unrelated business-logic changes — exactly as scoped.

### 16 — Channel Integration Foundation

**Status: COMPLETED**

- **`Conversation.channel` remains the one internal source of truth for channel type — never duplicated.** No second Conversation entity, no second internal channel enum was added. `ChannelConnection.type` (`ChannelType`: `TELEGRAM`/`WHATSAPP`/`WEBSITE`) describes an external account connection only, and is bridged to the existing `ConversationChannel` enum by one explicit, exhaustiveness-checked function (`channelTypeToConversationChannel()`) — never merged into one enum, since `ConversationChannel` also carries `MANUAL`/`PHONE`/`OTHER`, which have no channel-connection concept at all. Manually-created (`MANUAL`/`PHONE`/`OTHER`) conversations are entirely unaffected by this stage — verified live.
- **`ChannelConnection`** (`prisma/schema.prisma`, migration `20260910000000_channel_integration_foundation`) — a real external account connection, scoped to `Tenant`+`Business` (`Cascade`, matching every other domain table). `externalAccountId` is required (never nullable) specifically so `@@unique([tenantId, businessId, type, externalAccountId])` is a complete, unambiguous DB guarantee with no NULL-collapsing edge case to reason about. `config` is a small, non-secret JSON bag — `channelConnectionService.ts`'s `sanitizeChannelConfig()` strips any key that even *looks* credential-shaped (token/secret/key/password/credential/auth) and keeps only primitive values, never nested objects; the Zod schema enforces the "primitives only" shape independently as a second layer. New connections always start `INACTIVE`.
- **`Conversation` gained two nullable fields** (`channelConnectionId`, `externalConversationId`) plus `@@unique([channelConnectionId, externalConversationId])` — the same nullable-column unique-constraint technique already established for `AiEscalation.activeConversationId` (Prompt 12): Postgres never collapses two `NULL`s together, so every manually-created conversation is entirely unaffected, while a real (connection, external thread) pair gets a genuine, race-checkable DB guarantee. `ChannelConnection` deliberately has no `conversations ChannelConnection[]` relation treated as a source of truth for resolution — the unique index on `Conversation` itself *is* the resolution mechanism, one indexed lookup, no `ChannelMessage`→`Message`→`Conversation` join required.
- **`ChannelMessage`** maps one internal `Message` (`Cascade` — an owned child, same principle as `Message`→`Conversation`) to the external message that produced it — `@@unique([channelConnectionId, externalMessageId])` is the DB-enforced idempotency guarantee a redelivered webhook relies on. It stores no `content`/`direction`/`senderType` of its own — that data lives exactly once, in `Message`.
- **`CustomerChannelIdentity`** maps one external channel identity to a real `Customer` — `@@unique([channelConnectionId, externalCustomerId])`, always looked up scoped to `(tenantId, businessId, channelConnectionId)`, never by `externalCustomerId` alone (a raw external id from a different connection/business/tenant can never collide). Created only as a best-effort side effect *after* a message is durably recorded, never inside that same transaction — a genuine identity-linking conflict (two different customers resolving to the same external id) must never roll back a real inbound message that already succeeded.
- **A genuine, race-safety bug was found and fixed via a live concurrent run against the real Supabase database, not a unit test.** The initial inbound-conversation resolution used a plain `findFirst` → `create`, which is not race-safe: two concurrent "first messages" for a brand-new external thread could both miss the lookup and both attempt `create()`, and the loser would hit `Conversation`'s own unique constraint. Fixed by wrapping the `create()` in a try/catch for a real `P2002` and re-fetching the winner's row — the same "create, catch P2002, re-fetch" pattern already established in `escalationService.ts` (Prompt 12) — rather than switching to a `ChannelMessage`-mediated resolution path, which would have added an extra join for no additional safety (the DB constraint is what actually provides the guarantee either way). A second, unrelated bug was found the same way: the raw SQL phone-matching query's regex was written as `'\D'` inside a JS template literal, which JavaScript silently reduces to the single character `'D'` (confirmed: `` `\D`.length === 1 ``) — breaking the "strip non-digit characters" intent entirely. Fixed to `'\\D'`. Neither bug was catchable by a mocked unit test, since mocking the repository function or the whole transaction bypasses the exact code path (real Postgres concurrency; real regex string content) that was actually broken — this is the concrete reason this stage's real-Supabase smoke test step is not optional.
- **Customer resolution** (`channelCustomerService.ts`) is deliberately minimal: an existing `CustomerChannelIdentity` always wins (never re-matched by phone once linked — this is what makes identity reassignment impossible, not a runtime check); otherwise, exactly one active-customer match on a normalized "last 10 digits" phone heuristic resolves the conversation; zero or multiple matches leave it unresolved. No automatic `Customer` creation exists anywhere in this pipeline, no fuzzy name matching, no automatic merging.
- **Inbound pipeline** (`channelMessageService.ts`'s `receiveIncoming()`): validate the connection is `ACTIVE` → adapter `parseIncoming()` → idempotency pre-check → customer resolution → one atomic transaction (`channelInboundRepository.ts`'s `recordInboundMessage()`) that resolves/creates/reopens the `Conversation`, creates `Message(INBOUND, CUSTOMER)`, creates the `ChannelMessage` mapping, and touches `lastMessageAt`. AI is never invoked — no automatic reply, no escalation, no AI log entry from this stage.
- **Outbound foundation** (`sendOutbound()`): validates the target `Conversation` is genuinely linked to the specific `ChannelConnection` being used (never just same-tenant), calls the mock adapter, and — only on a reported success — records the message through the *existing* `messageRepository.createAndTouchConversation()` (`direction: OUTBOUND`, `senderType: STAFF`, never `AI`) — the same infra every manually-sent staff message already uses, so Dashboard analytics (Prompt 14) needs no changes at all.
- **Adapters are mocks only** — one factory (`createMockAdapter`) rather than three near-identical classes, selected by a `channelAdapterRegistry` keyed on `ChannelType`. No Telegram Bot API/WhatsApp Cloud API/Twilio/Meta API client, no OAuth, no webhook signature verification, no real network call anywhere in this stage.
- **API**: `GET/POST /api/channels`, `GET/PATCH /api/channels/:id`, `POST /api/channels/:id/activate|deactivate`, `POST /api/channels/:id/inbound` (explicitly marked in-code as a development/foundation endpoint, authenticated by the normal session cookie — not a production-secure webhook; real per-provider signature verification is Roadmap 17+ work). Owner/admin full access, manager read-only. `PATCH` never touches `status`/`tenantId`/`businessId`.
- Frontend: `/settings/channels` — a table (channel/display name/external account/status/created/actions), a create form with no credential field of any kind, edit, activate/deactivate. Manager sees "View only".
- Tests: `channel.schemas.test.ts` (21), `channelAdapters.test.ts` (5), `channelConnectionService.test.ts` (20), `channelCustomerService.test.ts` (10), `channelMessageService.test.ts` (16), `tenantIsolation.test.ts` gained a "Channel Integration" section (+21, including dedicated mocked coverage of both the message-level and conversation-level P2002 race-recovery paths). 1146 tests total, all passing.
- A full real-Supabase smoke test (two tenants, three channels for tenant A, one for tenant B, no mocks, 43 checks) verified the complete CRUD/activate/deactivate/permission matrix, the full inbound pipeline including duplicate idempotency and closed-conversation reopening (and that an *inactive* connection neither processes inbound nor reopens a closed conversation), known/unknown customer resolution via the real phone-normalization SQL, cross-tenant isolation on every mutation and on outbound, secret-shaped config rejection, and — the core of this stage after the mid-implementation fix — two genuinely concurrent `receiveIncoming()` calls for a brand-new external thread resolving to exactly one `Conversation` and one `Message`, never a raw database error. Full cleanup with a post-cleanup re-count confirming zero rows remain.
- **No existing Conversation/Message API was changed.** `GET/POST /api/conversations`, `GET/PATCH /api/conversations/:id`, and the message-send endpoint are byte-for-byte as Prompt 08 left them.
- No real Telegram/WhatsApp API, no real credentials, no webhook signature verification, no AI auto-reply, no new AI capabilities, no unrelated business-logic changes — exactly as scoped.

### 17 — Channel Operations & Delivery Foundation

**Status: COMPLETED**

- **`ChannelDelivery`** (`prisma/schema.prisma`, migration `20260909112328_channel_operations_delivery_foundation`) — tracks the outbound delivery lifecycle of exactly one internal `Message` on exactly one `ChannelConnection`. Fields: `id, tenantId, businessId, channelConnectionId, messageId, status (ChannelDeliveryStatus: PENDING/SENDING/SENT/FAILED, default PENDING), attemptCount, lastAttemptAt, deliveredAt, failedAt, errorCode, errorMessage, externalMessageId, createdAt, updatedAt`. Deliberately just those four statuses — no DELIVERED/READ/QUEUED/RETRYING invented for this foundation stage. `Tenant`/`Business` are `Cascade` (matching every other domain table); `ChannelConnection` is `Restrict` (same principle as `ChannelMessage`/`CustomerChannelIdentity` — delivery history must survive a connection being deactivated); `Message` is `Cascade` — a `ChannelDelivery` has no meaning independent of the `Message` it describes, the same "owned child" principle `ChannelMessage` already has toward `Message`. `@@unique([channelConnectionId, messageId])` is the spec's preferred at-most-one-active-delivery constraint; a second `@@unique([messageId])` is additionally required by Prisma for `Message`'s one-to-one `channelDelivery` reverse relation (the same reason `ChannelMessage` already carries two unique constraints).
- **Retry never creates a second row.** A `FAILED` delivery is retried by calling the same endpoint again — `attemptCount` increments on the *same* row (the `@@unique([channelConnectionId, messageId])` constraint makes a second row for the same pair impossible even under a race, not just an application-level convention).
- **Concurrent-send protection is a plain conditional `updateMany` compare-and-set — not an in-memory mutex, not `SERIALIZABLE` isolation.** `channelDeliveryRepository.ts`'s `claimForSending()` only transitions `PENDING`/`FAILED` → `SENDING` when its `updateMany` `WHERE` clause (id + current status) still matches at the moment Postgres applies the row lock; the loser's `WHERE` clause stops matching the instant the winner's `UPDATE` commits, so two truly concurrent claims can never both succeed. This works correctly across multiple serverless instances since the guarantee lives entirely in the database row, never in process memory — the same category of technique as `channelConnectionRepository.ts`'s `setStatus()`, one level more careful about the race. The row itself is found-or-created the same "create, catch a real `P2002`, re-fetch the winner" way already established in `escalationService.ts` (Prompt 12) and `channelInboundRepository.ts` (Prompt 16).
- **Idempotency**: a `SENT` delivery's claim short-circuits to `ALREADY_SENT` and the adapter is never called again — the existing successful result is simply returned. A `SENDING` claim (another request currently holds it) is rejected as `409 DELIVERY_IN_PROGRESS`, never a second concurrent adapter call.
- **Outbound pipeline** (`channelDeliveryService.ts`'s `sendMessageViaChannel()`): validate the `ChannelConnection` is `ACTIVE` (before anything else — no `ChannelDelivery` row is ever created for an inactive connection) → validate the `Message` exists, is tenant-scoped, and is `direction: OUTBOUND, senderType: STAFF` → validate its `Conversation` is genuinely linked to *this* `ChannelConnection` (never just same-tenant) → validate `Conversation.channel` matches the connection's type (defense-in-depth; structurally already guaranteed by the pipeline that sets both together) → validate an `externalConversationId` exists → claim the `ChannelDelivery` → call `adapter.sendMessage()` with a new `NormalizedOutboundMessage` (`channelType, externalConversationId, content` — never `tenantId`/`businessId`/`userId`/role/session; server-resolved only, never client input) → mark `SENT`/`FAILED`.
- **Every "wrong message" shape collapses to the same safe `404 MESSAGE_NOT_FOUND`** — a message from another conversation, another channel connection, a manually-created (`MANUAL`/`PHONE`/`OTHER`) conversation with no channel connection at all, an `INBOUND` message, a `CUSTOMER`-sent message, or another tenant entirely (`messageRepository.findById()` is already tenant-scoped). The response never distinguishes "exists elsewhere" from "doesn't exist."
- **Adapter errors are normalized to a small, safe internal shape** (`errorCode`, `errorMessage`, `retryable`) before ever reaching the client or the database — `channelDeliveryService.ts` maps every adapter failure (reported or thrown) to `502 CHANNEL_PROVIDER_ERROR` with a length-capped, safe message; a raw provider exception is caught and never leaks a stack trace or SDK error type. `ChannelSendResult` gained an optional `retryable` boolean (defaults to `true` when absent) — stored for future use, but this stage never acts on it: no automatic retry worker exists.
- **`NormalizedOutboundMessage` + adapter isolation from Prisma** (`src/server/channels/types.ts`) — the outbound mirror of `NormalizedIncomingMessage`. `ChannelAdapter.sendMessage()`'s signature was updated to take this type instead of Prompt 16's `ChannelSendInput`; the mock adapter (`adapters/mockAdapter.ts`) never imports Prisma, exactly as before — `Service → Repository` and `Service → Adapter` are the only edges, `Adapter → Prisma` remains structurally impossible. The mock's `'__mock_send_failure__'` magic-string failure trigger (Prompt 16) is preserved and now reports `retryable: true`, so the same string exercises the full `FAILED` → retry → `SENT` path.
- **API**: `POST /api/channels/:id/messages/:messageId/send` — `:id` = `ChannelConnection`, `:messageId` = an already-existing `Message`. The request body is never read (there is nothing for a client-supplied body to override — `tenantId`/`businessId`/`conversationId`/`customerId`/`externalConversationId` are all server-resolved from the URL and the existing `Message`/`Conversation`). Response is a flat `ChannelDeliveryDto` (`id, messageId, channelConnectionId, status, attemptCount, externalMessageId, lastAttemptAt, sentAt, errorCode, errorMessage, createdAt, updatedAt`) — never `tenantId`/`businessId`/config/secrets/a raw provider response. `sentAt` is the client-facing name for the internal `deliveredAt` column. Owner/admin/manager all get this operational access — the same precedent `receiveIncoming()` already established in Prompt 16 (channel *management* — create/edit/activate/deactivate a `ChannelConnection` — stays a separate, stricter owner/admin-only concern, untouched by this stage).
- **The pre-Prompt-17 `sendOutbound()` function (Prompt 16's "outbound foundation") was removed, not modified in place** — it was never wired to an API route (confirmed: no route file called it), had zero delivery-state tracking, zero retry semantics, and zero concurrent-send protection, which is exactly the gap this stage exists to fill. No client or frontend code ever called it, so removing it is not an observable behavior change for anything reachable through the API. Its replacement, `sendMessageViaChannel()`, reuses `channelMessageService.ts`'s existing `resolveActiveConnection()` helper (exported, not duplicated) for the identical "connection exists and is `ACTIVE`" check `receiveIncoming()` already relied on. `outboundChannelPayloadSchema` (also never wired to a route) was removed the same way.
- **The inbound pipeline (`receiveIncoming()`) is untouched** — no signature change, no behavior change; all of Prompt 16's inbound tests remain green unmodified.
- **`Conversation`/`Message` gained no new scalar columns** — `deliveryStatus`/`externalMessageId`/`channelType`/`providerStatus` were deliberately not added to `Message`; that state lives entirely in `ChannelDelivery`. Both models did gain a reverse-relation field with no new column (`Message.channelDelivery`, mirroring the existing `Message.channelMessage`) purely to satisfy Prisma's one-to-one relation requirement. `ConversationDto` gained one new field, `channelConnectionId` (always safe — null for a manual conversation), so the frontend can tell which connection an eligible message could be sent through without exposing `externalConversationId`.
- Frontend: `/settings/conversations`' existing detail panel (not `/settings/channels`, which has no messages to act on) gained a minimal "Send via channel" / "Повторить отправку" button next to each eligible `OUTBOUND`/`STAFF` message on a channel-linked conversation, plus a small status badge (status + attempt count, with the safe error message as a tooltip on failure). No AI send button, no auto-reply, no chat redesign, no new messaging surface.
- Tests: `channelDeliveryService.test.ts` is new (19 — every precondition rejection, the full success/failure lifecycle, idempotency, concurrent-send, retry, and a structural "no client-controlled ids" check); `tenantIsolation.test.ts` gained a "Channel Operations & Delivery" section (+10 — tenant-scoped repository lookups, cross-tenant `404`, and the full `claimForSending()` concurrent-send/race/retry matrix against the mocked real Postgres client); `channelMessageService.test.ts` lost its 5 `sendOutbound` tests (function removed) and kept all inbound tests green; `channelAdapters.test.ts` and `channel.schemas.test.ts` were updated for the `NormalizedOutboundMessage` shape and the removed `outboundChannelPayloadSchema`. 1170 tests total, all passing.
- A full real-Supabase smoke test (two tenants, no mocks, 27 checks) verified: outbound send creates a `ChannelDelivery`; a repeat send returns the same `SENT` result without a second adapter call; a forced mock failure produces `FAILED` with a safe error; a retry on the same row reaches `SENT` with the correct `attemptCount`; two genuinely concurrent sends (`Promise.allSettled`) produce exactly one adapter call and one `SENT` delivery, the loser rejected as `DELIVERY_IN_PROGRESS`; an inactive connection, a Telegram/WhatsApp channel-type mismatch, a cross-tenant message id, and a wrong-conversation/inbound/customer message are all rejected before any adapter call; no secret/Prisma/stack-trace content ever appears in a response; all of Prompt 16's inbound behavior (idempotency, concurrent first-inbound, customer identity, closed-conversation reopening, inactive-connection rejection) is unchanged. Full cleanup with a post-cleanup re-count confirming zero rows remain.
- No real Telegram/WhatsApp API, no webhook signature verification, no background retry worker, no queue infrastructure, no AI auto-reply, no AI-generated outbound messages, no unrelated business-logic changes — exactly as scoped.

### 18 — Real Telegram Channel Integration

**Status: COMPLETED**

- **Real Telegram Bot API connected for the first time — still no AI.** `ChannelType.TELEGRAM` (Prompt 16) is reused exactly as-is; no new enum, no `TelegramConversation`/`TelegramMessage`/`TelegramCustomer`. `TelegramChannelAdapter` (`src/server/channels/adapters/telegramAdapter.ts`) implements the existing `ChannelAdapter` interface (Prompt 16) — `parseIncoming()`/`sendMessage()` only, never touching Prisma/tenantId/businessId (spec §8). `telegramApiClient.ts` is the one place that speaks HTTPS to `api.telegram.org`, using exactly the five methods the spec allows (`getMe`, `setWebhook`, `deleteWebhook`, `getWebhookInfo`, `sendMessage`) via the platform's native `fetch` — no new HTTP-client dependency.
- **"Optional credential → mock fallback," the same convention `aiProviderFactory.ts` already established for `OPENAI_API_KEY`.** `channelAdapterRegistry.ts`'s `getChannelAdapter('TELEGRAM')` returns the real adapter only when `TELEGRAM_BOT_TOKEN` is configured server-side; otherwise it returns the exact same mock adapter Prompt 16/17 already used. This is why the entire pre-existing test suite — which defaults its example channel connections to `type: 'TELEGRAM'` — needed **zero changes** for this: it runs with no token configured, so it keeps exercising mock behavior byte-for-byte. The same pattern gates `channelConnectionService.ts`'s `activateChannelConnection()` (a TELEGRAM connection can only reach ACTIVE through `/telegram/setup` once a real token exists — otherwise the plain `/activate` endpoint works exactly as before) and `deactivateChannelConnection()` (best-effort `deleteWebhook()` only when a real token was ever in play).
- **The bot token and webhook secret never leave server-side environment configuration** (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET` — both optional getters on `env.ts`, matching `openAiApiKey`'s convention). Neither is ever written to Prisma, `ChannelConnection.config`, a DTO, a log line, or an error message — verified by dedicated tests and a live inspection pass (spec §26 security tests).
- **Bot identity**: `telegramSetupService.ts`'s `setupTelegramConnection()` calls `getMe()` and uses the bot's own immutable numeric id (never the mutable `@username`) as `ChannelConnection.externalAccountId` — reusing the existing Prompt 16 field and its `@@unique([tenantId, businessId, type, externalAccountId])` constraint exactly as designed, no new table. The bot's `@username` is stored only as a small, safe `config.telegramUsername` display value (via the existing, now-shared `sanitizeChannelConfig()`, extracted to `channels/sanitizeChannelConfig.ts` to avoid a require cycle between `channelConnectionService.ts` and `telegramSetupService.ts`).
- **Setup is all-or-nothing**: `getMe` → a cross-tenant "is this exact bot already active elsewhere" guard (`channelConnectionRepository.findOtherActiveByTypeAndExternalAccountId()` — see Limitations below for why this guard exists) → `setWebhook` against this connection's own unguessable webhook URL (`{APP_URL}/api/webhooks/telegram/:connectionId}`, HTTPS-only, rejected early with a clear `CHANNEL_CONFIGURATION_ERROR` otherwise) → only then does the connection become `ACTIVE`. A failure at any step leaves the connection exactly as it was — never partially activated.
- **Inbound webhook**: `POST /api/webhooks/telegram/:connectionId` (`api/webhooks/telegram/[connectionId].ts`) — a new, production-shaped endpoint, distinct from the untouched Prompt 16 foundation endpoint (`POST /api/channels/:id/inbound`, still session-cookie-authenticated, still a dev/manual-testing tool). Authenticated ONLY by Telegram's own `X-Telegram-Bot-Api-Secret-Token` header, checked with a constant-time comparison (`timingSafeCompare.ts`, SHA-256-digest-then-`timingSafeEqual`) against the single server-side `TELEGRAM_WEBHOOK_SECRET` — an invalid/missing secret never reaches a database query of any kind. `:connectionId` in the URL (set once, server-side, by `setWebhook` itself — never chosen by Telegram or any client) is what deterministically resolves the tenant/business, via a new, deliberately non-tenant-scoped `channelConnectionRepository.findByIdUnscoped()` used nowhere else. The route builds a synthetic, never-persisted `AuthContext` (`telegramWebhookContext.ts`) and hands the raw Telegram Update straight to the **unmodified** `receiveIncoming()` — no Conversation/Message logic duplicated in the route.
- **`receiveIncoming()`'s signature was widened, not rewritten**: `rawPayload` changed from the foundation endpoint's own `InboundChannelPayload` type to `unknown` — a pure type-level change (the function only ever forwarded this value into `adapter.parseIncoming()`, which already took `unknown`), letting the real Telegram Update JSON pass straight through with zero behavior change for the existing foundation caller.
- **A genuine concurrency bug in Prompt 16/17's own inbound-race recovery was found live and fixed.** The original `channelInboundRepository.ts` caught a `P2002` on `Conversation` creation and tried to re-fetch the winner **inside the same Postgres transaction** — this reliably fails against real Postgres with a second, unrelated `25P02` ("current transaction is aborted") error, because Postgres aborts an entire transaction on any statement error and Prisma's interactive transactions do not implicitly wrap each query in its own `SAVEPOINT`. This had never been caught before because it only manifests when two truly concurrent *different* first messages for the same brand-new external thread both miss the initial lookup and both attempt `create()` — a narrower interleaving than earlier concurrency tests happened to exercise. **Fixed** by removing the same-transaction catch entirely (`channelInboundRepository.ts` now lets a `Conversation`-level `P2002` propagate and roll back cleanly) and moving recovery to `channelMessageService.ts`'s `receiveIncoming()`, which already had to catch the `ChannelMessage`-level race: it first checks `loadDuplicateResult()` for a genuine duplicate delivery (same `externalMessageId` — the winner's row is guaranteed committed and visible, since Postgres only reports a unique-violation once the colliding transaction has fully committed), and — if that finds nothing, meaning the race was between two *different* messages — retries the whole `recordInboundMessage()` call once in a fresh transaction, which now finds the just-committed winning `Conversation` via its own plain lookup and proceeds normally. Neither this bug nor its fix required any change to `Conversation`/`Message`/`ChannelMessage` semantics. Confirmed fixed live against Supabase with the same genuinely-concurrent (`Promise.allSettled`) technique this project has used since Prompt 16.
- **Customer identity**: Telegram `from.id` → `externalCustomerId`; `chat.id` → `externalConversationId`; `{chatId}:{messageId}` → a deterministic `externalMessageId` (`telegram:{chatId}:{messageId}`) satisfying `ChannelMessage`'s existing `@@unique([channelConnectionId, externalMessageId])`. First/last name is passed through only as the existing `customerName` transient-metadata field (Prompt 16) — never written to `Customer.name`/`email`/`phone`. Telegram gives no phone number on a plain text message, so `channelCustomerService.ts`'s existing phone-based resolution simply never fires for Telegram in this stage — verified live: a Telegram customer stays unresolved (`customerId: null`, zero `CustomerChannelIdentity` rows) until linked by some other means, exactly the documented, honest behavior for the data Telegram actually provides.
- **Unsupported content is safely rejected, never fabricated.** Non-private chats (groups/supergroups/channels) and non-text messages (photos/voice/stickers/etc.) are normalized to an empty-`text` `NormalizedIncomingMessage` by `TelegramChannelAdapter.parseIncoming()` — the **existing**, unmodified `receiveIncoming()` validation (`!normalized.text` → `400 INVALID_CHANNEL_PAYLOAD`) rejects them exactly like any other invalid payload; the webhook route still acks Telegram with `200` (retrying would never help) but creates nothing. `parseIncoming()` never throws, matching the mock adapter's own established contract.
- **Outbound reuses the existing Prompt 17 `POST /api/channels/:id/messages/:messageId/send` — no second endpoint.** `ChannelSendResult` gained one new optional field, `errorCode` (Prompt 18), so `channelDeliveryService.ts` can persist/return an adapter-classified code (`TELEGRAM_AUTH_ERROR`/`TELEGRAM_BAD_REQUEST`/`TELEGRAM_NOT_FOUND`/`TELEGRAM_RATE_LIMITED`/`TELEGRAM_NETWORK_ERROR`/`TELEGRAM_PROVIDER_ERROR`) instead of the generic Prompt 17 fallback (`CHANNEL_PROVIDER_ERROR`, still used verbatim when an adapter — like the mock — doesn't classify) — purely additive, zero change to Prompt 17's own tests. A message over Telegram's real 4096-character limit is rejected before ever calling Telegram, never silently truncated. `ChannelDelivery`'s existing `PENDING → SENDING → SENT/FAILED` lifecycle, retry-reuses-the-same-row semantics, and compare-and-set concurrent-send guard (all Prompt 17, all unmodified) now drive a real Telegram send end-to-end — confirmed live, including a real `429` mapping to `FAILED`/`TELEGRAM_RATE_LIMITED` and a subsequent retry reaching `SENT` on the same row.
- **Frontend**: `/settings/conversations` (unchanged) is where an eligible message is actually sent, exactly as Prompt 17 left it. `/settings/channels` gained a Telegram-specific "Connect" action (calling the new setup endpoint instead of the generic Activate) and a small "Bot: @username" / "Webhook: configured" status line — never the token, secret, internal ids, or raw config JSON. Manager continues to see status only, per the existing permission model.
- Tests: `telegramAdapter.test.ts` (16), `telegramSetupService.test.ts` (13), `telegramWebhook.test.ts` (13), `telegramAiRegression.test.ts` (2) are new; `channelAdapters.test.ts` (+3), `channelDeliveryService.test.ts` (+1), `channelMessageService.test.ts` (+1, replacing the now-inaccurate same-transaction race test), and `tenantIsolation.test.ts` (+3, including the corrected `recordInboundMessage` race-propagation test) were extended. **1221 tests total, all passing.**
- A full real-Supabase smoke test (two tenants with two genuinely different mocked-network bot identities, no real Telegram credential available in this environment, 24 checks) verified: independent Telegram setup per tenant; the cross-tenant same-bot guard firing only for a genuine conflict; a real inbound flow through the actual webhook route (secret check → routing → `receiveIncoming()`) creating a real `Conversation`/`Message` with `channel = TELEGRAM`; no automatic `Customer`/`CustomerChannelIdentity` creation; duplicate and genuinely concurrent duplicate webhook deliveries converging on exactly one `Conversation`/`Message` (this is the run that caught and confirmed the `25P02` fix above); a real outbound send reaching `SENT` with Telegram's own `message_id` stored; a forced `429` reaching `FAILED`/`TELEGRAM_RATE_LIMITED` and a retry reaching `SENT` on the same row; inbound/outbound both rejected after deactivation; cross-tenant webhook isolation; a wrong secret rejected by the real route. Full cleanup with a post-cleanup re-count confirming zero rows remain.
- **Real Telegram smoke test: BLOCKED BY MISSING SERVER-SIDE CREDENTIAL.** No `TELEGRAM_BOT_TOKEN`/`TELEGRAM_WEBHOOK_SECRET` exists in this environment's `.env`. Per the prompt's own explicit instruction, none was invented and the user was not asked to paste one into chat — the real-network boundary was stubbed for the Supabase smoke test above instead (real DB, real service/route code, fake `fetch` responses standing in for `api.telegram.org`).
- **Zero Prisma migrations** — the existing `ChannelConnection`/`ChannelMessage`/`CustomerChannelIdentity`/`ChannelDelivery` schema (Prompts 16–17) already covered every Telegram requirement; `prisma validate`/`prisma migrate status` both confirm no schema drift.
- No real WhatsApp API, no AI auto-reply, no automatic booking, no background retry worker, no queue infrastructure, no unrelated business-logic changes — exactly as scoped.

### 19 — Frontend Foundation + App Shell

**Status: COMPLETED**

- **New design system — "Dark Graphite + Premium Gold" — replaces the original shadcn-default light palette project-wide.** `src/index.css`'s `:root` now holds the approved tokens (`background`/`foreground`/`card`/`primary`/`muted`/`accent`/`destructive`/`border`/`radius`) plus new semantic status tokens (`success`/`warning`/`info`) for system states only, never brand color. `--input`/`--ring` (needed by already-existing `Input`/`Button` components but absent from the approved snippet) were derived rather than invented: `--input` mirrors `--border`, `--ring` is the gold accent (a visible gold focus ring, as specified). `tailwind.config.js` maps the new tokens and adds an explicit `fontFamily.sans` fallback stack (`system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`) — Inter itself was never installed, and the spec explicitly said not to add a library "without necessity"; this is a single-theme (always-dark) system, not a light/dark toggle.
- **A real, product-wide App Shell now exists — applied to the entire application, not just new pages.** `src/components/layout/`: `AppShell.tsx` (Sidebar + Header + an independently-scrollable `<Outlet/>` main region, mounted once as a React Router v6 layout route), `Sidebar.tsx` (fixed-width, calm — background/border/gray text, gold reserved for the active-nav indicator only, never a gold sidebar background), `SidebarNav.tsx` (shared by desktop and mobile — a small gold indicator bar + gold icon + gold-tinted background on the active item, never a full-width solid button), `Header.tsx` (current-page title, a mobile menu trigger, deliberately minimal — no fake notifications), `MobileNav.tsx` (drawer + backdrop, closes on backdrop/Escape/link-click), `PageContainer.tsx`/`PageHeader.tsx` (the shared content-wrapper and title+subtitle block every page now uses instead of its own ad-hoc wrapper), `UserMenu.tsx` (a real, reusable account block reading the actual authenticated `user`/`business` from `AuthContext` — never invented placeholder personal data — with a working logout action; a static block rather than a popover, since no dropdown-menu primitive is installed and adding one wasn't necessary here), `navigation.ts` (the one source of truth for the sidebar's nav items and the header's page-title lookup). A new `src/components/ui/badge.tsx` (cva-based, `default`/`success`/`warning`/`info`/`gold`/`destructive` variants) is the prepared base for status badges.
- **Every existing page was migrated to the new shell, not just the new sections.** The old flat top `Nav.tsx` (and its own light-theme, no-sidebar navigation) is deleted — nothing imports it anymore. All 18 existing page components (`DashboardPage` + 17 `settings/*` pages) had their own `<div className="min-h-screen bg-muted/30"><Nav />...">` wrapper replaced with `<PageContainer>` (AppShell now supplies that chrome exactly once, globally); each page's actual business logic, data-fetching, and functionality is byte-for-byte unchanged — confirmed by the full pre-existing backend test suite staying green (it never touches frontend files) and a clean production build.
- **A real architecture conflict was found and resolved with the user before writing code** (spec's own explicit "flag conflicts, don't guess" rule): the prompt's 8-item nav model (Диалоги/Клиенты/Записи/AI-администратор/Передача сотруднику/Каналы, plus Dashboard/Settings) conceptually duplicated 12+ already-working pages built across Prompts 02–18, and its own placeholder instructions would have deleted the real, working Prompt 14 analytics dashboard. Resolved, confirmed by the user: (1) the five clearly-1:1 nav items lead directly to the real existing functionality (`/clients` renders the exact same `CustomersSettingsPage` that used to live at `/settings/customers`, etc. — one source of truth, the old URL redirects to the new one, never two parallel copies); (2) `/dashboard` is deliberately reset to the literal placeholder text the spec asked for, replacing Prompt 14's real analytics UI for now — a conscious product decision to rebuild it in the new visual system as its own future stage, not a side effect; the backend it depends on (`analyticsService.ts`, `GET /api/dashboard`) is completely untouched; (3) the new shell is the foundation for the *entire* product, not just the 8 new destinations. `/ai-admin` (no clean 1:1 existing match) was mapped to the existing `/settings/ai` "AI Core" test tool as the closest fit, and a new `SettingsHubPage` (`/settings`) links out to the eleven remaining, still-fully-functional `/settings/*` pages that aren't primary sidebar destinations (business/hours/services/knowledge/rules/vehicles/leads/service-history/customer-requests/ai-logs/team) — nothing became unreachable.
- **Routes** (React Router v6, one layout route wrapping every authenticated page in `AppShell`): `/dashboard`, `/conversations`, `/clients`, `/appointments`, `/ai-admin`, `/escalations`, `/channels`, `/settings`, plus the eleven unchanged `/settings/*` sub-pages and six legacy-URL redirects (`/settings/conversations|customers|appointments|ai|escalations|channels` → their new top-level route). `/login`/`/register` remain standalone, outside the shell, unchanged.
- **Responsive** (spec §22): Sidebar (`hidden lg:flex`) and MobileNav (`lg:hidden`) use the same `lg` breakpoint consistently, so there is no gap/overlap between the two; `AppShell`'s `overflow-hidden`/`overflow-y-auto` split keeps only the main content area scrollable. Verified by code review of every breakpoint-sensitive class (no live browser/screenshot tool is available in this environment) — no page introduces a fixed pixel width capable of forcing horizontal overflow at 390px.
- **Accessibility** (spec §23): semantic landmarks (`<aside>`, `<nav aria-label>`, `<header>`, `role="dialog" aria-modal` for the drawer); every icon-only control (`Header`'s menu button, `MobileNav`'s close button, `UserMenu`'s logout button) has `aria-label`; all interactive controls are real `<button>`/`<a>` elements, never a clickable `<div>`; visible `focus-visible:ring-2 focus-visible:ring-ring` (the gold accent) on every nav link and icon button; MobileNav closes on Escape.
- No business logic was implemented in any of the new/placeholder surfaces (spec §24) — Dashboard's placeholder and SettingsHubPage's links are the only new "content"; no fake statistics, customers, conversations, or appointments were invented anywhere.
- **Nothing on the backend was touched** — confirmed via `git diff --stat` showing zero changes under `src/server/`, `api/`, or `prisma/`; the full 1221-test backend suite passes unchanged, `prisma validate`/`migrate status` were not even necessary to re-run (no schema edited).
- A known, deliberately-accepted cosmetic nit: the inner JSX of the 18 migrated pages sits one indentation level deeper than it strictly needs to now that one `<div>` wrapper collapsed into `<PageContainer>` — functionally and visually irrelevant, but not re-flowed, since no code-formatter (e.g. Prettier) is configured in this project and a manual mass re-indent across 18 large files was judged higher-risk than the cosmetic benefit.
- No AI chat, Telegram/WhatsApp integration UI, booking logic, CRM logic, customer/conversation CRUD, analytics, billing, or notification backend was implemented — exactly as scoped.

## Current

**Status: 20 — Production Hardening is CURRENT / NEXT IMPLEMENTATION.**

Every stage through Frontend Foundation + App Shell (01–19) is verified
complete in the code. Prompt 20 (security review, rate limiting, logging,
monitoring, backups, production deployment) has not been started.

## Future Roadmap

### 20 — Production Hardening

**Goal**: security review, rate limiting (a basic limiter already exists from Prompt 01 — `src/server/middleware/rateLimit.ts` — this stage means a full production-grade pass), logging, monitoring, error handling, backups, reliability, performance, production deployment, privacy, audit.

## Development Rule

Each future stage must, before writing any code:

1. Read `PRODUCT_BLUEPRINT.md`.
2. Read `DEVELOPMENT_ROADMAP.md` (this document).
3. Read `AI_BEHAVIOR_CONTRACT.md`.
4. Verify the actual current state of the code — never assume a prior document is still accurate.
5. Implement **only** the current stage — never pre-build a future stage's models/endpoints/UI.
6. Run the test suite.
7. Run TypeScript checking.
8. Run the production build.
9. Run a real-database smoke test whenever the stage touches the database, with full cleanup afterward.
10. Stop after the current stage — do not auto-proceed to the next one.

## Documentation Source of Truth

This document describes the intended product architecture.

For already implemented functionality: the current repository code and
database schema are the source of truth.

For future functionality: this document and `PRODUCT_BLUEPRINT.md` define
the intended direction.
